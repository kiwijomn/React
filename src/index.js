import React from 'react';
import ReactDOM from 'react-dom/client';
import PassingCount from './chapter_09_1/PassingCount.jsx';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <PassingCount/>
);
